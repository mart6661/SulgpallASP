@model IEnumerable<Domain.Commentar>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Messageboard.Title)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Player.Firstname)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.BoardCommentar)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Messageboard.Title)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Player.Firstname)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.BoardCommentar)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.CommentarId }) |
            @Html.ActionLink("Details", "Details", new { id=item.CommentarId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.CommentarId })
        </td>
    </tr>
}

</table>
